import {Home} from "home.slint";
import {Playlists, PlaylistsScreen} from "playlists.slint";
import {Search, SearchScreen, Album, AlbumScreen} from "search.slint";
import {Settings} from "settings.slint";

export {Home, Playlists, Search, Settings, SearchScreen, Album, AlbumScreen, PlaylistsScreen}