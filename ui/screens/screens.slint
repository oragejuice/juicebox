import {Home} from "home.slint";
import {Playlists} from "playlists.slint";
import {Search, SearchScreen} from "search.slint";
import {Settings} from "settings.slint";

export {Home, Playlists, Search, Settings, SearchScreen}